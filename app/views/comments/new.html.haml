.container
  %ul
    - @comment.errors.full_messages.each do |message|
      %li= message

  = form_with(model: @comment, url: article_comments_path(@comment.article), local: true) do |f|
    %div
      = f.label :content, I18n.t('articles.content')
    %div
      = f.text_area :content
    = f.submit I18n.t('articles.save'), class: 'btn-primary'
